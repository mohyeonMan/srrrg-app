# srrrg-k3s 작업 지침

이 저장소는 srrrg를 필요한 기능부터 다시 옮기기 위한 초기 Spring Boot 앱이다. 현재 제품 기능과 DB 스키마는 없다. 기존 구현은 별도 `srrrg` 저장소를 참고한다.

## 작업 범위: 반드시 지킬 규칙

- 사용자가 승인한 목표와 변경 파일을 이번 작업의 경계로 삼는다. 승인받지 않은 파일은 생성·수정·삭제하지 않는다.
- 작업 중 다른 문제를 발견해도 임의로 고치지 않는다. 목표 달성에 추가 변경이 필요하면 파일과 이유·영향을 먼저 설명하고 명시적으로 승인받은 뒤 진행한다.
- 수정 전의 변경 상태를 기록하고, 완료 전 실제 변경 파일을 승인된 목록과 대조한다. 범위 밖 변경이 있으면 완료했다고 보고하지 말고 원인을 확인한다.
- 지침이나 관례가 현재 사용자의 명시적 요청과 충돌하면 추측으로 범위를 넓히지 말고 충돌을 설명한다.

## 판단 원칙

- 사용자 제안이나 기존 구현을 무조건 옳다고 가정하지 않는다. 요구사항, 현재 코드, 검증 가능한 근거와 트레이드오프를 바탕으로 독립적으로 판단한다.
- 더 나은 선택이나 문제점이 있으면 동의만 하지 말고 이유와 대안을 분명히 설명한다. 자신의 제안에도 같은 기준을 적용하고, 근거가 바뀌면 판단을 수정한다.
- 판단에 이견이 있어도 승인된 범위를 임의로 바꾸지 않는다. 설계 변경이 필요하면 영향과 선택지를 제시하고 사용자 결정을 받는다.

## 작업 절차

- 작업 전에 현재 파일과 `git status --short`를 읽기 전용으로 확인한다.
- 조사와 구현을 분리하고, 목표·범위·변경 파일·검증·위험을 적은 계획을 제시한다. 사용자가 계획을 명시적으로 승인하기 전에는 수정하지 않는다.
- 구현 계획에는 사용할 기술 스택과 각 선택의 이유, 고려한 대안과 선택하지 않은 이유를 명시한다.
- 승인된 범위만 변경한다. 범위를 넓혀야 하면 이유와 영향을 설명하고 다시 승인받는다.
- 사용자 파일과 로컬 설정을 덮어쓰지 않는다. `.env`, 인증서, 키, 토큰 원문은 명시적 요청 없이 읽거나 출력하지 않는다.
- 요청 없이는 Git stage, commit, push, 브랜치·태그 생성 또는 이력 변경을 하지 않는다.
- 코드 작성·수정 전 `docs/conventions.md`의 공통 규칙을 확인한다.
- 코드 변경 시 책임과 제약을 설명하는 한국어 주석 및 현재 동작을 설명하는 문서를 함께 갱신한다.
- 관련 테스트와 빌드를 실행하고, 실행하지 못한 검증과 남은 위험을 보고한다.

## 현재 기준

- Java 21, Spring Boot 4.0.3, Gradle wrapper를 사용한다.
- 상태 검사는 `/actuator/health/readiness`에서 제공한다.
- PostgreSQL과 Redis는 로컬 테스트용 Compose 서비스로만 준비되어 있다. 앱의 저장소 연결, 인증, 제품 API 및 자동 배포는 아직 없다.
- 여러 파드에서 실행할 기능을 옮길 때는 파드 간 공유 상태와 중복 실행을 먼저 설계한다.
